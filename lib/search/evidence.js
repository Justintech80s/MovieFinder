const clamp=n=>Math.max(0,Math.min(1,Number.isFinite(Number(n))?Number(n):0));
export function createEvidenceRecord(input={}){return {kind:input.kind||'metadata',source:input.source||'unknown',claim:String(input.claim||''),confidence:input.confidence==null?1:clamp(input.confidence),path:Array.isArray(input.path)?input.path:[],metadata:{...(input.metadata||{})}};}
export function dedupeEvidence(records=[]){const seen=new Set();return records.map(createEvidenceRecord).filter(r=>{const key=`${r.kind}|${r.source}|${r.claim}|${JSON.stringify(r.path)}`;if(seen.has(key))return false;seen.add(key);return true;});}
